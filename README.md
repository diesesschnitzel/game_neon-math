# Neon Einmaleins

Ein Arcade-Spiel zum Üben des kleinen Einmaleins. React + Vite + TypeScript.

## Lokal starten

**Voraussetzung:** Node.js

1. Dependencies installieren: `npm install`
2. Dev-Server starten: `npm run dev`
3. Build: `npm run build`
